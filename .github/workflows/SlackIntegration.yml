name: SendToSlack

# on:
#   schedule:
#     # * is a special character in YAML so you have to quote this string
#     - cron:  '*/30 * * * *' 
    
on: push #Used for Debug

jobs:
  build:

    runs-on: macOS-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: Run stackapi install
      run: pip install stackapi
    - name: Run check of pwd
      run: pwd
    - name: Run a one-line script
      run: python ./.github/workflows/StackOverflow.py ${{ secrets.JuliaLangSlackWebHook }}
      
# We can use git commands to log if FireBase does not pan out.       
#     - name: Run git add check
#       run: git add .
#     - name: Run git commit check
#       run: git commit -m "Autoupdate of Logs"
#     - name: Run git push check
#       run: git push
